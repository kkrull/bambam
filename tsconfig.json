//https://aka.ms/tsconfig
{
  "extends": "@tsconfig/node18/tsconfig.json",
  "compilerOptions": {
    //TODO KDK: Add baseUrl?
    //features/tsconfig has its own paths, but cucumber-js still needs this
    "paths": {
      "@/src/*": ["./src/*"],
      "@/support/*": ["./features/support/*"]
    }
  },
  "include": ["./src/**/*.ts"]
}

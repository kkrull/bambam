import { Given, Then, When } from '@cucumber/cucumber';

Given('I have exported a MIDI Format 1 file from my DAW', () => 'pending');

When('I ask BamBam to write a copy of that source file', () => 'pending');

Then('the output file should be a valid MIDI file', () => 'pending');

Then('the output file should have the same format as the source file', () => 'pending');

Then('the output file should have the same number of tracks as the source file', () => 'pending');

Then('the output file should have the same time division as the source file', () => 'pending');

Then('the output file should have the same track names as the source file', () => 'pending');

Then('the output file should have the same track events as the source file', () => 'pending');
